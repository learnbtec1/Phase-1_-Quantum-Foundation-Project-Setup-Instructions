[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: slow tests (optional filter)
    e2e: integration / WebSocket pipeline; opt-in: pytest -m e2e (skipped in default CI)
