'use client';

import React from 'react';

/**
 * Minimal placeholder for holographic panels in the boardroom.
 * Returns null to avoid breaking the scene when the full component is missing.
 */
export default function HolographicPanels() {
  return null;
}
