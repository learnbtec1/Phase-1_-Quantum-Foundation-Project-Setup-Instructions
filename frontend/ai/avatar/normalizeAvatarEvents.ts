/**
 * Re-export — canonical implementation lives in `@/utils/events/normalizeAvatarEvents`.
 */
export {
  dispatchAvatar,
  normalizeAvatarEvent,
  type EmotionEventDetail,
  type EmotionEventInput,
  type GestureEventDetail,
  type GestureEventInput,
  type GestureToken,
  type ListeningEventDetail,
  type ListeningEventInput,
} from '@/utils/events/normalizeAvatarEvents';
